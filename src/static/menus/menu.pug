script.
    const electron = require("electron");
    const {
        ipcRenderer
    } = electron;
    $(document).ready(() => {
        
        $('.menu-btn').click(function() {
            ipcRenderer.send('menu:click-option', $(this).data('command'));
        });
        
    });
div.noty_message
  span.noty_text
div.container
    div.row
        for option in Object.keys(data)
            button.menu-btn.col-md-10.offset-md-1(data-command=(data[option].command || data[option]))= option
div.noty_close
